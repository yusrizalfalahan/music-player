#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/electron$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/electron$exe"  "$basedir/../electron-spawn/cli.js" $args
  } else {
    & "$basedir/electron$exe"  "$basedir/../electron-spawn/cli.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "electron$exe"  "$basedir/../electron-spawn/cli.js" $args
  } else {
    & "electron$exe"  "$basedir/../electron-spawn/cli.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
